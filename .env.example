PORT=4000
# Replace <username>, <password>, <cluster-host>, and <database> with your Atlas credentials & database name.
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/<database>?retryWrites=true&w=majority
# Use a strong random value here in your real .env (never commit it)
JWT_SECRET=replace_me
NODE_ENV=development
LOG_LEVEL=info
